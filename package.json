{
  "name": "my-turborepo",
  "version": "0.0.0",
  "private": true,
  "description": "",
  "author": "",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:deps": "docker compose -f docker-compose.deps.yml up -d",
    "dev": "pnpm run prepare:deps && turbo run dev --concurrency=20",
    "dev:auth": "pnpm run prepare:deps && turbo run dev --filter api-gateway --filter \"@repo/shared-types\" --filter auth",
    "dev:no-web": "pnpm run prepare:deps && turbo run dev --filter api-gateway --filter auth --filter games --filter wishlists --filter \"@repo/shared-types\" --filter \"@repo/prisma\"",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "husky": "^9.1.7",
    "prettier": "^3.2.5",
    "turbo": "^2.8.0"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  }
}
